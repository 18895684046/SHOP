import request from '../index'
import config from '../config'

const {
    address: {
        setDefaultAddress,
        delAddress,
        addressDetail,
        defaultAddress,
        addressList,
        addAddress,
        updateAddress,
    }
} = config
